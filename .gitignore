# Rust build artifacts
/target/
**/*.rs.bk

# Cargo lock (keep for binaries, omit for libraries — this workspace has both)
# Cargo.lock is intentionally checked in for reproducible builds

# Environment / secrets
.env
.env.local
*.env

# IDE / editor
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Flamegraph / profiling output
perf.data
perf.data.old
flamegraph.svg
*.profraw
*.profdata

# Benchmark baselines (Criterion stores HTML reports in target/)
criterion/

# Node / JS build artifacts (applies to dashboard/ and any future JS packages)
node_modules/
dist/
.vite/
*.tsbuildinfo

# Python build artifacts (all locations)
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
dist/

# Rust + maturin Python extension build
bindings/python/target/
bindings/python/*.so
bindings/python/*.pyd

# napi-rs Node extension build
bindings/typescript/*.node
bindings/typescript/target/

# Docker / observability scratch data
examples/observability/data/

# Generated docs (mdBook build output)
book/
docs/book/

# Claude related
.claude/
CLAUDE.md
PLAN.md
SPEC.md